- if current_user
  p.font-20.pink= "Share this campaign #{pluralize(number_of_shares(topic), 'more time')}"
- else
  p.font-20.pink= "Share this campaign #{pluralize(topic.num_of_shares, 'more time')}"